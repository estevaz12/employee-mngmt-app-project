<% flash.each do |name, msg| %>
  <div data-alert data-options="animation_speed:500, animation:'fadeOut';"
       class="alert-box radius <%= flash[:notice] ? "success" : "warning" %> z-depth-2">
    <%= msg %>
  </div>
<% end %>
